package answer.king;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

/**
 * This class is a base test class and needs to be extended by other test classes.
 * Common app related initialization (before/after tests, etc) can be added to this class.
 * @author Pushkar
 *
 */

@RunWith(SpringRunner.class)
@SpringBootTest
public class AppBaseTest {	

	//Add test life-cycle methods if required
}
